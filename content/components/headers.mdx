---
title: Headers
---

import {Caption} from '@primer/gatsby-theme-doctocat'
import Header from '../../images/Headers.png'
import GhPrList from '../../images/gh-pr-list.png'

When viewing output that could be unclear, headers can quickly set context for what you’re seeing and where you are.

### Examples
<img src={Header} alt="An example of the header of the gh pr create command" />
<Caption>The header of the "gh pr create" command reassures the user that they're creating the correct pull request</Caption>

<img src={GhPrList} alt="An example of the header of the gh pr list command" />
<Caption>The header of the "gh pr list" command sets context for what list you're seeing</Caption>
