---
title: Lists
---
import {Caption} from '@primer/gatsby-theme-doctocat'
import List from '../../images/gh-pr-list.png'

Lists use tables to show information. State is shown in color. A header is used for context. Information shown may be branch names, dates, or whatever is most relevant in context.

<img src={List} alt="An example of gh pr list" />
