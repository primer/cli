---
title: Lists
---

import List from '../../images/gh-pr-list.png'

Lists use tables to show information. State is shown in color. Each has a fuzzy date sorted by recently updated. A header is used for context.

<img src={List} alt="" />
